import { siteConfig } from "@/config/site";

export function getDirectorySummary() {
  return `Browse ${siteConfig.niche} by area, rating, cuisine, takeaway, delivery, halal options, vegetarian options, and dining style.`;
}
