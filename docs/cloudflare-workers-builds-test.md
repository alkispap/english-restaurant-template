# Cloudflare Workers Builds test

This documentation-only change verifies the automatic deployment path: an approved GitHub merge to `main` triggers Cloudflare Workers Builds without any local Wrangler upload.
